<%= form_with(model: dumpster, local: true) do |form| %>
  <% if dumpster.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(dumpster.errors.count, "error") %> prohibited this dumpster from being saved:</h2>

      <ul>
      <% dumpster.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <%= form.hidden_field :post, @post["id"] %>

  <%= form.text_area :comment, class: "form-text-field", size: "100x5", placeholder: "Tell us your reasons..." %>
  <br><br>
  <div class="actions">
    <%= form.submit 'Send', class: 'btn btn-outline-info' %>
  </div>
<% end %>
